name: admin
http-port: 8080
runmode: dev
table-prefix: gab_
db:
  - name: default
    driver: mysql
    dsn:
    log: runtime/log/mysql/default.log
    max-idle-conn: 5
    max-open-conns: 30
    show-sql: true